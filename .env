# === LLM Provider Selection ===
# Choose ONE provider: 'openai', 'google', or 'ollama' (default if not set)
LLM_PROVIDER="ollama"

# === API Keys (only needed for the selected provider) ===
# Get key from https://platform.openai.com/api-keys
OPENAI_API_KEY="your-openai-api-key-here"

# Get key from Google AI Studio: https://aistudio.google.com/app/apikey
GOOGLE_API_KEY="your-google-api-key-here"

# === Model Names (Optional - defaults are set in the script) ===
# You can override the default models used for each provider
# MODEL_NAME="gpt-4" # Example for OpenAI
# MODEL_NAME="gemini-1.5-pro-latest" # Example for Google
# MODEL_NAME="llama3" # Example for Ollama

# === Ollama Configuration (Only needed if using Ollama) ===
# Default URL for the Ollama API (usually correct if running locally)
# OLLAMA_URL="http://localhost:11434/api/generate"
# OLLAMA_CHECK_URL="http://localhost:11434" # URL for checking if server is up

# === File Paths (Optional - defaults are set in the script) ===
# MEMORY_FILE="agent_memory.json"
# DREAM_FILE="agent_dreams.md"
